<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    app:layout_behavior="@string/appbar_scrolling_view_behavior"
    tools:context=".ui.activities.chat.ChatActivity">

    <Button
        android:id="@+id/allUsersButton"
        android:layout_width="@dimen/dimen_180"
        android:layout_height="@dimen/dimen_70"
        android:text="@string/allUsersButton"
        android:layout_below="@+id/calendarButton"
        android:layout_alignLeft="@+id/googleMapAnimation"
        android:layout_alignStart="@+id/googleMapAnimation" />

    <android.support.v7.widget.RecyclerView
        android:id="@+id/recycler_view"
        android:layout_below="@+id/allUsersButton"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:scrollbars="vertical" />
</RelativeLayout>
